# Copy this file to .env and fill in your values.
# .env is gitignored — never commit secrets.

# Anthropic API key — required only when invoking agents with --use-llm.
# Without this key, all agents fall back to deterministic template backends.
ANTHROPIC_API_KEY=your_key_here
